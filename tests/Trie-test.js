import { expect } from 'chai';
import { Trie } from '../lib/Trie.js';
import { Node } from '../Node.js';

describe('Trie', () => {

  it.skip('it should be ', () => {
    expect(Trie).to.be.a('object');
  })

});